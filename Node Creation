#include<stdio.h>
#include<stdlib.h>
struct node
{
    int data;
    struct node*link;
};
int main()
{
    int d;
    struct node *head=malloc(sizeof(struct node));
    printf("enter data");
    scanf("%d",&d);
    head->data=d;
    head->link=NULL;
    printf("%d" ,head->data);
    return 0;
}
